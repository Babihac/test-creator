{
  "name": "echo-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tailwindcss -i assets/css/styles.css -o assets/css/tailwind.css --minify",
    "watch": "tailwindcss -i assets/css/styles.css -o assets/css/tailwind.css --watch",
    "react-build": "esbuild --bundle react/index.tsx --outdir=assets/js --minify --sourcemap --global-name=bundle --watch",
    "dev": "npm-run-all --parallel watch react-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "daisyui": "^4.7.1",
    "esbuild": "0.20.1"
  }
}
